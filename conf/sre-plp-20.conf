--sample-frequency=16000
--frame-length=25 # the default is 25.
--low-freq=40 # the default.
--num-mel-bins=23
--lpc-order=19
--high-freq=-200 # the default is zero meaning use the Nyquist (8k in this case).
--num-ceps=20 # higher than the default which is 12.

